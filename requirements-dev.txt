# ============================================================
# Local development requirements
# ============================================================
# Install with: pip install -r requirements-dev.txt
#
# Includes everything in requirements.txt PLUS:
#   - torch + transformers for running models locally
#   - psycopg2-binary for PostgreSQL
#   - FastAPI + uvicorn for API layer
#   - All testing and linting tools
# ============================================================

# Everything from production
-r requirements.txt

# Local LLM inference (NOT used on Streamlit Cloud)
torch>=2.0.0
transformers>=4.40.0

# PostgreSQL driver (needs C build tools — not available on Streamlit Cloud free tier)
psycopg2-binary==2.9.9

# API layer (for local/Docker deployments)
fastapi>=0.110.0
uvicorn>=0.29.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Code quality
black>=24.0.0
ruff>=0.4.0
mypy>=1.9.0
