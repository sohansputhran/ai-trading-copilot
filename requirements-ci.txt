# CI-only requirements
# ----------------------
# Deliberately minimal — no torch, no transformers, no yfinance.
# These are heavy packages (torch alone is 2GB+) that would make CI
# take 5+ minutes and blow the free tier cache limits.
#
# test_agents.py needs: pytest, structlog, pydantic
# test_orchestrator.py needs: the above + langgraph
# Both test files have zero HuggingFace/torch dependencies by design.

# Core agent framework (lightweight — no ML models)
langgraph>=0.2.0
langchain>=0.3.0

# Data
pandas>=2.0.0
numpy>=1.26.0

# Logging
structlog>=24.1.0

# Utilities
pydantic>=2.6.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Linting (also used in lint job, kept here for local convenience)
ruff>=0.4.0
black>=24.0.0
