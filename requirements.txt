# ============================================================
# Production requirements — Streamlit Cloud deployment
# ============================================================
# Keep this file LEAN. Streamlit Cloud installs every package
# listed here on every deploy. Heavy packages (torch, 2GB+)
# will cause the installer to time out and fail.
#
# For local development with torch/transformers, use:
#   pip install -r requirements-dev.txt
# ============================================================

# Core agent framework
langgraph>=0.2.0
langchain>=0.3.0
langchain-community>=0.3.0

# HuggingFace Inference API (HTTP calls only — no local models)
# DO NOT add torch or transformers here; they're 2GB+ and break cloud builds.
huggingface-hub>=0.22.0

# Data pipeline
yfinance>=0.2.36
pandas>=2.0.0
numpy>=1.26.0

# UI
streamlit>=1.32.0
plotly>=5.20.0

# Logging & utilities
structlog>=24.1.0
python-dotenv>=1.0.0
pydantic>=2.6.0
tenacity>=8.2.0